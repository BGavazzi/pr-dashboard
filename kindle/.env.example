# Fine-grained PAT, read-only: Pull requests (read) + Contents (read) on the
# repos you want visible. Do NOT use a token with write scope — this server
# never needs to write, and it has no auth in front of it on your LAN.
GH_TOKEN=
